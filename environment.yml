name: gaussian_splatting
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - cudatoolkit=11.6
  - plyfile
  - python=3.7.13
  - pip=22.3.1
  # - pytorch=1.12.1
  # - torchaudio=0.12.1
  # - torchvision=0.13.1
  - tqdm
  - pip:
    # 新增
    - torch==1.12.1+cu116
    - torchvision==0.13.1+cu116
    - torchaudio==0.12.1
    - -f https://download.pytorch.org/whl/torch_stable.html
    - opencv-python
    - joblib
    #--------------------------------------------------------#
    # - submodules/diff-gaussian-rasterization // 額外安裝、跟隨torch安裝會找不到torch的library
    # - submodules/simple-knn
    # - submodules/fused-ssim

